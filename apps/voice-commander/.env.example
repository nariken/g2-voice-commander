# Personal sideload only — keys are embedded in the client bundle.
# Gemini API key: https://aistudio.google.com  (Get API key)
VITE_GEMINI_API_KEY=
# Linear personal API key (needs write): Linear → Settings → Security & access → Personal API keys
VITE_LINEAR_API_KEY=
