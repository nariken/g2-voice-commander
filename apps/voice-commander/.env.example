# This app ships NO provider keys — they live in the proxy (see ../../proxy).
# Point it at your deployed Worker, and use the same APP_TOKEN you set on the Worker.
VITE_PROXY_URL=https://g2-voice-commander-proxy.<your-subdomain>.workers.dev
VITE_APP_TOKEN=
